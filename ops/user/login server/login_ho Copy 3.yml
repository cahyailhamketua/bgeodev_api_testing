info:
  name: login_ho Copy 3
  type: http
  seq: 6

http:
  method: POST
  url: https://epatrol.bgeodev.cloud/api/login
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: headbuj2
      - name: password
        type: text
        value: headoffice123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
