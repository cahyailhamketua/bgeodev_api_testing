info:
  name: login_ho Copy
  type: http
  seq: 4

http:
  method: POST
  url: https://epatrol.bgeodev.cloud/api/login?username=headbuj&password=ho
  params:
    - name: username
      value: headbuj
      type: query
    - name: password
      value: ho
      type: query
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: satusolid2
      - name: password
        type: text
        value: satusolid123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
