info:
  name: login_anggota sdt (test)
  type: http
  seq: 15

http:
  method: POST
  url: https://epatrol.bgeodev.cloud/api/login
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: budiyanto
      - name: password
        type: text
        value: budiyanto123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
