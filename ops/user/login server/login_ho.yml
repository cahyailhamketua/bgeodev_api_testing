info:
  name: login_ho
  type: http
  seq: 3

http:
  method: POST
  url: https://epatrol.bgeodev.cloud/api/login
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: satusolid2
      - name: password
        type: text
        value: satusolid123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
