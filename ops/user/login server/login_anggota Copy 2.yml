info:
  name: login_anggota Copy 2
  type: http
  seq: 14

http:
  method: POST
  url: https://epatrol.bgeodev.cloud/api/login
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: anggotarizq
      - name: password
        type: text
        value: anggota123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
