info:
  name: login_anggota Copy
  type: http
  seq: 13

http:
  method: POST
  url: https://epatrol.bgeodev.cloud/api/login
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: anggotabuj1.2
      - name: password
        type: text
        value: anggota123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
