info:
  name: login_anggota (local)
  type: http
  seq: 5

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: anggotaofc1.1
      - name: password
        type: text
        value: anggota123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
