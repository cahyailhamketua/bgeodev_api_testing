info:
  name: login_danru (local)
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: danruofc1.1
      - name: password
        type: text
        value: danru123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
