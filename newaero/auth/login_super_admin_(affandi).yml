info:
  name: login_super_admin_(affandi)
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "affandi",
        "password": "123123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
